---
apiVersion: v1
kind: query
spec:
  name: CDK - Npm Packages
  description: Return the list of npm packages
  query: |
    SELECT
      *
    FROM
      npm_packages;
  interval: 3600
  logging: snapshot
  observer_can_run: true
  automations_enabled: false
  discard_data: false
