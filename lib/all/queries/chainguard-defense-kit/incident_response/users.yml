---
apiVersion: v1
kind: query
spec:
  name: CDK - Users
  description: Returns a list of users
  query: |
    SELECT
      *
    FROM
      users
  interval: 3600
  logging: snapshot
  observer_can_run: true
  automations_enabled: false
  discard_data: false
