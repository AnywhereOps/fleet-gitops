---
apiVersion: v1
kind: query
spec:
  name: CDK - Hardware Events
  description: Return hardware events
  query: |
    SELECT
      *
    FROM
      hardware_events;
  interval: 3600
  logging: snapshot
  observer_can_run: true
  automations_enabled: false
  discard_data: false
  team: both
