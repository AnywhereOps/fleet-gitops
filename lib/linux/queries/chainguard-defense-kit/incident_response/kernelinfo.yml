---
apiVersion: v1
kind: query
spec:
  name: CDK - Kernel Info
  platform: linux
  description: Return basic kernel information
  query: |
    SELECT
      *
    FROM
      kernel_info;
  interval: 3600
  logging: snapshot
  observer_can_run: true
  automations_enabled: false
  discard_data: false
