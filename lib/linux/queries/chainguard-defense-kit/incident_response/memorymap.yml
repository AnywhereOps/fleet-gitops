---
apiVersion: v1
kind: query
spec:
  name: CDK - Memory Map
  platform: linux
  description: Returns the OS memory region map.
  query: |
    SELECT
      *
    FROM
      memory_map;
  interval: 3600
  logging: snapshot
  observer_can_run: true
  automations_enabled: false
  discard_data: false
